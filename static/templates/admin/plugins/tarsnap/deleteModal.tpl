<p class="lead">
	Please select the archive(s) you wish to delete from your Tarsnap account. <code>Ctrl</code> or <code>Shift</code> click to select multiple entries.
</p>

<div class="form-group">
	<select class="form-control" multiple id="tarsnap-delete-modal-archive-list">
		<!-- BEGIN archives -->
		<option value="{@value}">{@value}</option>
		<!-- END -->
	</select>
	<p class="help-block">
		Don&apos;t see any archives? You probably need to hit the "List" button first, and then re-open this modal.
	</p>
</div>